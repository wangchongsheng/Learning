1、Form是什么东西？
	用于验证用户请求数据合法性的一个组件
2、Django的Form的实现步骤：
	a. 创建一个验证用户请求的模板
	b. 获取用户请求，进行验证
		- is_valid()
		- clean()
		- errors
	c. 
		Form提交：
			errors.字段.0
		Ajax提交：
			errors.as_json()
			errors.as_data()
			{'user':[Django对象(),]}
			
		
			