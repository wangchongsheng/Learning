函数
模块
面向对象编程

1.深浅拷贝
    浅拷贝=只拷贝第一层
    深拷贝=克隆一份

指针关系

2.set(集合)
    把不同的元素组合到一起，如果有重复的只会读取一个。
    集合的创建方式只有一种
    特点：无序，不重复
    作用：
        1.去重。把一个列表变成集合，就自动去重
        2.关系测试。
            交集
            并集
            差集
集合的对象是一组无序排列的可哈希的值，集合成员可以做字典的键。


函数
#数学函数 ！= function（）
#计算机函数 == subroutine 子程序，procedures 过程
作用：
    1.减少重复代码
    2.方便修改，更易扩展
    3.保持代码一致性
函数的命名规则：
    1.函数名必须是以下划线或字母开头，可以包含任意字母，数字或下划线的组合。
    2.区分字母大小写
    3.不能使用保留字

调用函数一定要记得加括号
加了（*）的参数是无命名参数，（**）的参数是有命名参数

函数的参数：
1.关于不定长参数位置：无命名参数（*args）放在左边，有命名参数**kwargs参数放在右边
2.如果存在默认参数，放左边
def info(sex='male',*args, **kwargs):
    print(args)
    print(sex)
    for i in kwargs:
        print('%s:%s' % (i, kwargs[i]))

参数优先级，从左到右
def func(keyword,key=15,*args,**kwargs)

函数的返回值（return）
作用：
    1.结束函数
    2.返回某个对象
注意点：
    1.执行语句中只要遇到return，就结束并执行返回结果。
    2.函数里如果不存在return，会默认返回一个None.
    3.如果return多个对象，那么python会把多个对象封装成一个元组数据返回

函数的作用域：
在python中if不存在作用域，而函数是存在作用域的。

作用域的4种情况：
    L:local,局部作用域
    E:enclosing,嵌套作用域
    G:global,全局变量
    B:built-in,函数系统内置固定变量,例如：int,float

1.变量查找顺序：:L--E--G--B
2.只有模块、类‘及函数才能引入新作用域
3.对于一个变量，内部作用域先声明就会覆盖外部变量，不声明直接使用，就会使用外部作用域的变量；
4.内部作用域要修改外部作用域变量值时，全局变量要使用global关键字，嵌套作用于变量要使用nonlocal关键字。

高阶函数
#1.函数名可以进行赋值
#2.函数名可以做函数参数，还可以做为函数的返回值

