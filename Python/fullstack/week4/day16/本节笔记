
装饰函数中重要的三个函数
1.函数的作用域：
在python中if不存在作用域，而函数是存在作用域的。

作用域的4种情况：
    L:local,局部作用域
    E:enclosing,嵌套作用域
    G:global,全局变量
    B:built-in,函数系统内置固定变量,例如：int,float

1.变量查找顺序：:L--E--G--B
2.只有模块、类‘及函数才能引入新作用域
3.对于一个变量，内部作用域先声明就会覆盖外部变量，不声明直接使用，就会使用外部作用域的变量；
4.内部作用域要修改外部作用域变量值时，全局变量要使用global关键字，嵌套作用于变量要使用nonlocal关键字。

2.高阶函数
#1.函数名可以进行赋值
#2.函数名可以做函数参数，还可以做为函数的返回值

3.闭包
闭包=内部函数+定义函数时的环境
1.闭包就是一个内部函数

装饰器函数：
    作用：为其他函数增加功能